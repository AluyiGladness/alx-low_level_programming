#include "main.h"

/**
 * print_number - prints number
 *
 * @n: integer to print
 *
 * Return: returns void
 */
void print_number(int n)
{
	unsigned int j;

	j = n;
	if (n < 0)
	{
		_putchar(45);
		j = -n;
	}
	if (x / 10)
	{
		print_number(x / 10);
	}
	_putchar((x % 10) + '0');
}

