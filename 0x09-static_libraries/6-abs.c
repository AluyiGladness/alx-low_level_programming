#include "main.h"
/**
 * _abs - gives the absolute value
 *
 * @n: integer to make absolute
 *
 * Return: int or int multiplied by negative
 *
 */
int _abs(int n)
{

	if (n > 0)
	{
		return (n);
	}
	return (n * -1);
}

