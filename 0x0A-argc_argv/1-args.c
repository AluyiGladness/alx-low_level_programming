#include <stdio.h>
/**
 * main - prints the number of argument a user pass into it
 * @argc: argument count of programme
 * @argv: array of arguments ( array of pointers)
 * Return: 0
 */
int main(int argc, char **argv __attribute__((unused)))
{
	printf("%d\n", argc);
	return (0);

}
